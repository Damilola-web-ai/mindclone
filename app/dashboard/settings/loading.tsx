import { DashboardWorkspaceLoading } from "@/components/dashboard/workspace-loading";

export default function SettingsLoading() {
  return <DashboardWorkspaceLoading statCount={3} leftCards={2} rightCards={1} />;
}
